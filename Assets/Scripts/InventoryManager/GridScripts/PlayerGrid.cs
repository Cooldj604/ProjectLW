using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerGrid : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        GridObject grid = new GridObject(4, 3, 10f);
        
    }

}
